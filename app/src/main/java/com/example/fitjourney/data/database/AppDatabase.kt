package com.example.fitjourney.data.database

import androidx.room.RoomDatabase

abstract class AppDatabase : RoomDatabase() {
    // Qui definisci i DAO, per esempio:
    // abstract fun activityDao(): ActivityDao
}
